#include "Leg.h"

Leg::Leg(int id, float rotation, float alignGroundAngle, int coxaPin, int femurPin, int tibiaPin)
    : id(id), rotation(rotation), alignGroundAngle(alignGroundAngle), coxaPin(coxaPin), femurPin(femurPin), tibiaPin(tibiaPin) {}
